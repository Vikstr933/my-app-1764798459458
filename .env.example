# API Configuration
VITE_API_URL=http://localhost:3001/api

# OpenAI Configuration (if using client-side API calls - NOT RECOMMENDED)
# VITE_OPENAI_API_KEY=your_openai_api_key_here

# Note: For security, OpenAI API calls should be made from the backend server
# The backend server should have its own .env file with OPENAI_API_KEY
